from manager.models import MedicalRecord, Patient; from django.db import connection; print('Tables in database:', connection.introspection.table_names()); patient = Patient.objects.first(); print(f'Patient: {patient}'); if patient: new_record = MedicalRecord(patient=patient, complaint='Test complaint'); new_record.save(); print(f'Created medical record ID: {new_record.id}'); print(f'Total MedicalRecord count: {MedicalRecord.objects.count()}')
